syntax = "proto3";

package Dadtkv;


service LeaseManagerService {
  rpc RequestLeases(LeaseRequest) returns (LeaseResponse);
}

message LeaseRequest {
  string transaction_manager_id = 1;
  repeated string requested_dadints = 2;
}

message LeaseResponse {
  repeated Lease lease = 1;
}

message Lease {
  string transaction_manager_id = 1;
  repeated string dadints = 2;
}
